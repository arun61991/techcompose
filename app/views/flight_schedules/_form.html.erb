<%= form_with(model: flight_schedule, local: true) do |form| %>
  <% if flight_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight_schedule.errors.count, "error") %> prohibited this flight_schedule from being saved:</h2>

      <ul>
        <% flight_schedule.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flight_id %>
    <%= form.number_field :flight_id %>
  </div>

  <div class="field">
    <%= form.label :departure_airport_id %>
    <%= form.number_field :departure_airport_id %>
  </div>

  <div class="field">
    <%= form.label :destination_airport_id %>
    <%= form.number_field :destination_airport_id %>
  </div>

  <div class="field">
    <%= form.label :date %>
    <%= form.datetime_select :date %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
